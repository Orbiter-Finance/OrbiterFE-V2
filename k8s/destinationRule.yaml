apiVersion: networking.istio.io/v1
kind: DestinationRule
metadata:
  name: orbiter-bridge-front-destination
  namespace: orbiter-bridge-test
spec:
  host: orbiter-bridge-front-service
  subsets:
    - name: prod
      labels:
        version: prod
    - name: preview
      labels:
        version: preview
  trafficPolicy:
    loadBalancer:
      consistentHash:
        httpHeaderName: 'x-forwarded-for'
